<%= simple_form_for(current_user) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :bio %>
    <%= f.input :city %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
